# Optional: base URL for the notes backend API.
# If unset, the app will call same-origin endpoints (e.g., /notes).
REACT_APP_API_BASE=http://localhost:8000

# Alternative supported variable name (used if REACT_APP_API_BASE is unset)
REACT_APP_BACKEND_URL=http://localhost:8000
